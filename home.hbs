<style type="text/less">
	@import 'airok-mixins.less';
	#content
	{
		display: flex;
		align-items: center;
	}
	.register-form
	{
		flex-grow: 1;
		.margin-top-bottom(40px);

		& > [step-name]
		{
			transition: opacity .2s;
		}
		&:not([current-step="route"]) > [step-name="route"]
		{
			opacity: 0;
		}
		[step-name="route"]
		{
			text-align: center;
		}
		.pitch-line
		{
			margin-bottom: 40px;
			.padding-left-right(70px);
			line-height: 2em;
			font-size: 24px;
			font-weight: lighter;

			@media (max-width: 600px)
			{
				font-size: 18px;
			}
		}
		.fields-area
		{
			.padding-top-bottom(40px);
			background-color: rgba(255, 255, 255, 0.4);
		}
		.origin-field,
		.destination-field
		{
			display: inline-block;
			width: 400px;
		}
		.origin-field-label,
		.destination-field-label
		{
			display: block;
			font-size: 30px;
			font-weight: 200;
		}
		[name="origin"],
		[name="destination"]
		{
			width: 200px;
			margin-top: 40px;
		}
		.next-button
		{
			margin-top: 40px;
			color: black;
			text-decoration: none;
		}
	}
</style>
<form method="get" action="/register" current-step="route" class="register-form">
	<div step-name="route">
		<div class="centering-padder">
			<p class="pitch-line">
				Já pensou viajar sem ter que se preocupar com passagens? A gente sim.
				Procuramos 24 horas por dia pela melhor passagem para você
				enquanto você arruma as malas e se prepara para a viagem ;)
			</p>
		</div>
		<div class="fields-area">
			<label class="origin-field">
				<span class="origin-field-label">
					Onde você está?
				</span>
				<select name="origin">
					<option>Porto Alegre, RS</option>
				</select>
			</label>
			<label class="destination-field">
				<span class="destination-field-label">
					Para onde gostaria de ir?
				</span>
				<select name="destination">
					<option>Campina Grande, PB</option>
				</select>
			</label>
		</div>
		<a class="next-button btn btn-default" onclick="$(this).parents('.register-form').attr('current-step', 'dates')">
			Escolher datas
		</a>
	</div>
</form>
